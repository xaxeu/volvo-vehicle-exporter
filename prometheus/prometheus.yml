global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'volvo-vehicle-exporter'

scrape_configs:
  - job_name: 'volvo-exporter'
    static_configs:
      - targets: ['localhost:9100']
    scrape_interval: 30s
